# 更新日志

## v1.7.1 — 2026-09-18

- 小番茄格式的 Gilbert 曲线序生成改为并行：先按"子块格子数 = 该块产生的下标个数"把曲线切成
  若干块并算出各块起始下标（各块区间互不重叠、顺序与串行一致），再交给多线程同时生成。
  1080p 下曲线生成 29 → 18 ms、小番茄整体 39 → 23 ms；结果与串行逐位一致。
  这条路径的收益不如其他格式，是因为它的耗时主要在递归切分本身而不是写像素。
- 增加耗时日志（tag `混淆图`）：记录解码结果与耗时，以及每次处理的
  「取像素 / 算法 / 位图 / PNG 编码 / 写标记 / 合计」分段时间，便于定位大图慢在哪一步。

## v1.7 — 2026-09-18

- 大图处理全面加速：所有格式改为多核并行 + 热路径优化，结果与 v1.6 逐位一致
  （`dev/ParityTest` 对 7 种格式 × 加解密 × 1-4 次 × 多尺寸共 3212 例全绿）。
  真机 8 MP 实测：自动识别 2452 → 593 ms（4.1×），PicEncrypt 行列 2137 → 322 ms（6.6×），
  行像素混淆 183 → 13 ms（13.9×），本机密钥流混淆 310 → 81 ms（3.8×）。
- 具体做法：按行/列/像素区间切分给多个线程（各切片输出互不重叠）；
  本机格式把「通道旋转 + 密钥流异或」合并成一次遍历，并用字节偏移让每段各自定位 ChaCha20 密钥流；
  Logistic 逐行/逐列置换改为各段跳过 j×(w-1) 步独立推进，省掉每行重复分配缓冲；
  去掉内层循环的取模、复用 MD5 实例、Gilbert 曲线按固定步长写入。
- 自动识别不再给每个候选格式都克隆一份原图，省掉最多 6 次整图拷贝。
- 没上 GPU 的原因见 README「性能」一节：这些算法是离散访存型置换，多核 CPU 更划算。

## v1.6 — 2026-09-17

- 格式选择改为应用自绘的 MIUI 风格下滑面板（圆角顶角、拖拽条、行内说明、蓝色对勾、滑入动画），
  不再使用系统原生 `AlertDialog`；选格式时会清掉 PNG 标记里的格式，以手动选择为准。

## v1.5 — 2026-09-17

- 修复界面内图标丢失圆角：顶栏与关于页的图标改用白色圆角底板 + `clipToOutline`。

## v1.4 — 2026-09-17

- 体积从 535 KB 降到 93.9 KB：不再生成传统多档图标 PNG（minSdk 26 起系统只用自适应图标），
  自适应前景改为单档 WebP q92；签名只保留 v2/v3。

## v1.3 — 2026-09-17

- 修复「一直提示图片太大」：改为两遍流式解码，不再把整张原图读进内存，取消文件体积上限；
  报错按真实原因区分（权限 / 云相册未下载 / 相机 RAW / 解码失败 / 内存不足），并把底层异常写入日志（tag `混淆图`）。
- 像素预算：混淆 800 万、还原自适应放宽到 1200 万；还原时若被迫缩小会提示可能无法还原。

## v1.2 — 2026-09-17

- 修复分段控件点按方向相反的问题：真实触摸不再触发 `performClick()` 的「切到下一项」，改为按点击的格子选中。

## v1.1 — 2026-09-16

- 新增网络通用格式：小番茄（Gilbert 曲线）、PicEncrypt 行列/行模式、行像素、全像素、方块混淆，共 6 种；
- 还原页支持自动识别格式（相邻像素灰度差评分，必要时自动尝试 2 次）；
- PNG `tEXt` 标记升级到 `v=2`，记录格式、次数与原始尺寸（方块混淆还原时自动裁掉补齐边缘）；
- 与参考实现、小番茄官网逻辑完成双向逐像素对拍。

## v1.0 — 2026-09-16

- 首个版本：ChaCha20 密钥流 + 行列洗牌的混淆/还原，MIUI 风格界面，自适应图标（图标由作者插画生成）。
